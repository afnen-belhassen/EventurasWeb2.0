{% extends 'base.html.twig' %}
{% block title %}<h1 id="title-eve" + {{ event.title }}> </h1></span>{% endblock %}
{% block body %}
<style>
    /* Add this style block to make all form labels white */
    .form-label {
        color:  #0056b3!important;
    }
    
    /* If your forms use a different class for labels, adjust accordingly */
    label {
        color: #0056b3 !important;
    }
   
    .event-form {
            background-color: #f8f9fa; /* Light background for the form */
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        .nav-link{
            color:white !important;
        }
        text-center{
            font-size:20 px !important;
        }

</style>

<nav class="navbar navbar-expand-lg">
        <div class="container">
            <a class="navbar-brand" href="{{ path('app_home') }}">
                Eventura
            </a>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav align-items-lg-center ms-auto me-lg-5">
                    <li class="nav-item">
                        <a class="nav-link click-scroll" href="#section_4">Contact</a>
                    </li>
                </ul>

                <div class="d-flex gap-2">
                   
                     {{ include('service/sidebar.html.twig') }}
                </div>
            </div>
        </div>
    </nav>

<div class="container mt-5">
    <div class="event-form">
    <h1 class="text-center mb-3">Modifiez l'évenement: {{ event.title }}</h1>

    {{ form_start(form) }}
        {{ form_row(form.title) }}
        {{ form_row(form.date_event) }}
        {{ form_row(form.date_finEve) }}
        {{ form_row(form.prix) }}
        {{ form_row(form.location) }}
        {{ form_row(form.description) }}
        {{ form_row(form.category) }}
        {{ form_row(form.image) }}
        {{ form_row(form.activities) }}
        {{ form_row(form.nb_places) }}
        <button type="submit" class="btn btn-primary">Sauvegarder</button>
        <a href="{{ path('app_show_all_events') }}" class="btn btn-secondary">Annuler</a>
    {{ form_end(form) }}
    </div>
</div>
{% endblock %}
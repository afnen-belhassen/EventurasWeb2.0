{% extends 'base.html.twig' %}

{% block title %}Reclamations Back Office{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="row">
        <div class="col-12">
            <h1 class="mb-4">Reclamations Back Office</h1>
            
            <div class="card">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center mb-4">
                        <h5 class="card-title">Reclamations Management</h5>
                        <a href="{{ path('app_reclamations_statistics') }}" class="btn btn-primary">
                            <i class="fas fa-chart-bar me-2"></i>View Statistics
                        </a>
                    </div>
                    
                    <!-- Add your reclamations table or list here -->
                    <p class="text-muted">No reclamations found.</p>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}

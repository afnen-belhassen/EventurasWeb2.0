{% extends 'base.html.twig' %}

{% block title %}Reservation Confirmed{% endblock %}

{% block body %}
<div style="max-width: 800px; margin: 0 auto; padding: 40px; text-align: center;">
    <h1 style="color: #4CAF50;">🎉 Reservation Confirmed!</h1>
    <div style="background: white; padding: 30px; border-radius: 10px; margin-top: 30px; box-shadow: 0 4px 8px rgba(0,0,0,0.1);">
        <h2>Your Ticket Details</h2>
        {# In your reservation_success.html.twig #}
        <p><strong>Event:</strong> {{ reservation.event.title }}</p>
        <p><strong>Seat Number:</strong> {{ reservation.ticket.seat_number }}</p>
        <p><strong>Ticket Code:</strong> {{ reservation.ticket.ticket_code }}</p>
        <p><strong>Date:</strong> {{ event.date_event|date('F j, Y') }}</p>
        
        <div style="margin-top: 30px; padding: 20px; background: #f8f9fa; border-radius: 5px;">
            <p>A confirmation has been sent to your email.</p>
            <p>Please present this ticket code at the venue.</p>
        </div>
        
        <a href="{{ path('app_home') }}" style="display: inline-block; margin-top: 30px; padding: 10px 20px; background: #6feaf6; color: white; text-decoration: none; border-radius: 5px;">Back to Home</a>
    </div>
</div>
{% endblock %}
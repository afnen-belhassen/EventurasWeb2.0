{% extends 'organizer/base.html.twig' %}

{% block title %}Create Partnership{% endblock %}

{% block organizer_content %}
    <section class="hero-section" id="section_1">
        <div class="section-overlay"></div>

        <div class="container d-flex justify-content-center align-items-center">
            <div class="row">
                <div class="col-12 mt-auto mb-5 text-center">
                    <h1 class="text-white mb-5">Créer un Partenariat</h1>
                    <a class="btn custom-btn smoothscroll" href="#section_2">Voir le Formulaire</a>
                </div>

                <div class="col-lg-12 col-12 mt-auto d-flex flex-column flex-lg-row text-center">
                    <div class="date-wrap">
                        <h5 class="text-white">
                            <i class="custom-icon bi-clock me-2"></i>
                            Nouveau Partenariat
                        </h5>
                    </div>

                    <div class="location-wrap mx-auto py-3 py-lg-0">
                        <h5 class="text-white">
                            <i class="custom-icon bi-geo-alt me-2"></i>
                            Partenaires
                        </h5>
                    </div>

                    <div class="social-share">
                        <ul class="social-icon d-flex align-items-center justify-content-center">
                            <span class="text-white me-3">Share:</span>
                            <li class="social-icon-item">
                                <a href="#" class="social-icon-link"><span class="bi-facebook"></span></a>
                            </li>
                            <li class="social-icon-item">
                                <a href="#" class="social-icon-link"><span class="bi-twitter"></span></a>
                            </li>
                            <li class="social-icon-item">
                                <a href="#" class="social-icon-link"><span class="bi-instagram"></span></a>
                            </li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <section class="about-section section-padding" id="section_2">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-lg-10">
                    <div class="card shadow-lg" style="background-color: #1a2035;">
                        <div class="card-header" style="background-color: #2c2e33; border-bottom: 1px solid #e9ecef;">
                            <h1 class="mb-0 text-white text-center"><i class="fas fa-handshake me-2"></i>Formulaire de Partenariat</h1>
                        </div>
                        <div class="card-body">
                            {{ form_start(form, {'attr': {'class': 'needs-validation', 'novalidate': 'novalidate'}}) }}
                                <div class="row">
                                    <div class="col-md-6">
                                        <div class="mb-3">
                                            {{ form_label(form.partnerId, 'Partenaire', {'label_attr': {'class': 'form-label text-white'}}) }}
                                            {{ form_widget(form.partnerId, {'attr': {'class': 'form-control bg-dark text-white border-secondary'}}) }}
                                            {{ form_errors(form.partnerId, {'attr': {'class': 'text-danger'}}) }}
                                        </div>

                                        <div class="mb-3">
                                            {{ form_label(form.contracttype, 'Type de Contrat', {'label_attr': {'class': 'form-label text-white'}}) }}
                                            {{ form_widget(form.contracttype, {'attr': {'class': 'form-control bg-dark text-white border-secondary'}}) }}
                                            {{ form_errors(form.contracttype, {'attr': {'class': 'text-danger'}}) }}
                                        </div>
                                    </div>

                                    <div class="col-md-6">
                                        <div class="mb-3">
                                            {{ form_label(form.description, 'Description', {'label_attr': {'class': 'form-label text-white'}}) }}
                                            {{ form_widget(form.description, {'attr': {'class': 'form-control bg-dark text-white border-secondary', 'rows': '5'}}) }}
                                            <div class="form-text text-white-50">
                                                <span id="charCount">0</span>/200 caractères
                                            </div>
                                            {{ form_errors(form.description, {'attr': {'class': 'text-danger'}}) }}
                                        </div>

                                        <div class="mb-3">
                                            {{ form_label(form.status, 'Statut', {'label_attr': {'class': 'form-label text-white'}}) }}
                                            {{ form_widget(form.status, {'attr': {'class': 'form-control bg-dark text-white border-secondary'}}) }}
                                            {{ form_errors(form.status, {'attr': {'class': 'text-danger'}}) }}
                                        </div>
                                    </div>
                                </div>

                                <div class="d-flex justify-content-end mt-4">
                                    <button type="submit" class="btn" style="background-color: #bd4c33; color: white; border: none;">
                                        <i class="fas fa-save me-1"></i>Créer le Partenariat
                                    </button>
                                </div>
                            {{ form_end(form) }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}

{% block javascripts %}
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const descriptionField = document.querySelector('#partnership_description');
            const charCount = document.querySelector('#charCount');
            
            function updateCharCount() {
                const length = descriptionField.value.length;
                charCount.textContent = length;
                if (length > 200) {
                    charCount.classList.add('text-danger');
                } else {
                    charCount.classList.remove('text-danger');
                }
            }
            
            descriptionField.addEventListener('input', updateCharCount);
            updateCharCount(); // Initial count
        });
    </script>
{% endblock %} 